import expect from 'expect'
import { testMe } from '../src'

describe('is_undefined', () => {
  it('is a starting point for your tests, have at it', () => {
    expect(testMe(1,1)).toEqual(2)
  })
})
